{% extends 'client/index.html.twig' %}
            {% block body %}
                <main role="main" class="col-md-10 offset-1 col-lg-10">
                    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                        <h2><i class="fa fa-angle-right"></i> Create User</h2>
                    </div>
                    <div class="jumbotron content-form-client">
{#                        formulaire de client_user, récupérer l'email du client user et creer le compte user avec#}


                        {{ form_start(form) }}
                        <div class="form-group">

                            <div class="col-xs-6">
                                {{ form_row(form.clientUser.name) }}
                                {{ form_row(form.clientUser.email) }}
                                {{ form_row(form.email) }}
                                <div style="display: none">
                                    {{ form_row(form.Client) }}
                                    {{ form_row(form.clientUser.projects) }}
                                </div>
                            </div>
                        </div>

                        <div class="form-group">
                            <div class="col-xs-6">
                                {{ form_row(form.password) }}
                            </div>
                        </div>
                        <button class="btn btn-primary">{{ button|default('Send') }}</button>
                        {{ form_end(form) }}
                    </div>

                    </div>
                </main>
            {% endblock %}

{% block javascripts %}

{% endblock %}